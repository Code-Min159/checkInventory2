package com.workExample.checkInventory.dao;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public class PackageMapper {
    // This interface will contain methods for CRUD operations on Package data.
    // For example:
    // List<Package> findAll();
    // Package getById(String id);
    // Package createPackage(Package request);
    // Package updatePackage(Package request);
    // void deletePackage(String id);
    // List<Package> searchByName(String name);



    // Note: The actual SQL queries and method implementations will be defined in the corresponding XML mapper file.
}
